// This component is no longer in use.
import React from 'react';

const SimpleFishIcon: React.FC<React.SVGProps<SVGSVGElement>> = (props) => (
  <svg {...props}></svg>
);

export default SimpleFishIcon;